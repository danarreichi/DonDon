<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dondon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DonDon">
        <activity android:name=".edit_transaksi_expense"></activity>
        <activity android:name=".edit_transaksi_income" />
        <activity android:name=".tambah_kategori_expense" />
        <activity android:name=".tambah_kategori_income" />
        <activity android:name=".tambah_transaksi_expense" />
        <activity android:name=".tambah_transaksi_income" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>